library(rpart)
library(rpart.plot)
library(rattle)

car.uci <- read.csv(url("http://archive.ics.uci.edu/ml/machine-learning-databases/car/car.data"), header=FALSE)

colnames(car.uci) = c("buying", "maintenance", "doors", "persons", "lug_boot", "safety", "evaluation")

head(car.uci)

prp(crs$rpart, cex=1, varlen=0, branch=0)

# rattle()
